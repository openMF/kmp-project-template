CLAUDE-PLHLD-v1
# Placeholder for macOS installer .p12 cert. Export from Keychain Access → My Certificates → Export.
# Real file goes to secrets/desktop/macos/installer.p12 (NEVER commit). See README.md.
