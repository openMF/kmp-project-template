# =============================================================================
# secrets-manifest.yaml — GENERATED by core/scripts/secrets-generate-manifest.sh
# =============================================================================
# Source of truth: deployment/<platform>/<target>/secrets-needs.yaml#vault_aliases
# Framework entry point per RESEARCH D17 (no template hardcode).
schema_version: "3.0"
project: mifos-x/kmp-project-template
default_account_email: null

needs:
  - alias: mifos-x-vercel-token
    canonical: env:VERCEL_TOKEN
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-vercel-org-id
    canonical: env:VERCEL_ORG_ID
    materialize: { format: "env", via: "export" }
  - alias: kmp-project-template-vercel-project-id
    canonical: env:VERCEL_PROJECT_ID
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-cloudflare-api-token
    canonical: env:CLOUDFLARE_PAGES_API_TOKEN
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-cloudflare-account-id
    canonical: env:CLOUDFLARE_ACCOUNT_ID
    materialize: { format: "env", via: "export" }
  # FRED developer API key — client secret embedded in BuildConfig (env_var_client → local.properties,
  # resolved via secrets-resolve-path). core/network/build.gradle.kts reads it into BuildKonfig.FRED_API_KEY;
  # the "fred" access point in app-profile/app.yaml references this alias. A fork provides the value with
  # `/secrets push mifos-x-fred-api-key` (free key: https://fred.stlouisfed.org/docs/api/api_key.html).
  - alias: mifos-x-fred-api-key
  - alias: mifos-x-netlify-auth-token
    canonical: env:NETLIFY_AUTH_TOKEN
    materialize: { format: "env", via: "export" }
  - alias: kmp-project-template-netlify-site-id
    canonical: env:NETLIFY_SITE_ID
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-ms-partner-center-tenant-id
    canonical: env:MS_PARTNER_CENTER_TENANT_ID
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-ms-partner-center-client-id
    canonical: env:MS_PARTNER_CENTER_CLIENT_ID
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-ms-partner-center-client-secret
    canonical: env:MS_PARTNER_CENTER_CLIENT_SECRET
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-msstore-app-id
    canonical: env:MS_APP_ID
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-msstore-identity-name
    canonical: env:WINDOWS_MSIX_IDENTITY_NAME
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-msstore-publisher
    canonical: env:WINDOWS_MSIX_PUBLISHER
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-msstore-publisher-display-name
    canonical: env:WINDOWS_MSIX_PUBLISHER_DISPLAY_NAME
    materialize: { format: "env", via: "export" }
  - alias: github_release_upload_token
    canonical: env:GH_TOKEN
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-appstore-auth-key-p8
    canonical: "secrets/live/apple/appstore/AuthKey.p8"
    materialize: { format: "file", via: "copy" }
  - alias: mifos-x-appstore-key-id
    canonical: "secrets/live/apple/appstore/key_id"
    materialize: { format: "file", via: "copy" }
  - alias: mifos-x-appstore-issuer-id
    canonical: "secrets/live/apple/appstore/issuer_id"
    materialize: { format: "file", via: "copy" }
  - alias: mifos-x-match-cert-bundle
    canonical: "secrets/live/apple/match/match_ci_key"
    materialize: { format: "file", via: "copy" }
  - alias: mifos-x-match-password
    canonical: "secrets/live/apple/match/.match_password"
    materialize: { format: "file", via: "copy" }
  - alias: mac_app_store_cert_p12
    canonical: "secrets/live/desktop/macos/app_store.p12"
    materialize: { format: "file", via: "copy" }
  - alias: mac_installer_cert_p12
    canonical: "secrets/live/desktop/macos/installer.p12"
    materialize: { format: "file", via: "copy" }
  - alias: azure_ts_tenant_id
    canonical: env:AZURE_TS_TENANT_ID
    materialize: { format: "env", via: "export" }
  - alias: azure_ts_client_id
    canonical: env:AZURE_TS_CLIENT_ID
    materialize: { format: "env", via: "export" }
  - alias: azure_ts_subscription_id
    canonical: env:AZURE_TS_SUBSCRIPTION_ID
    materialize: { format: "env", via: "export" }
  - alias: azure_ts_account_name
    canonical: env:AZURE_TS_ACCOUNT_NAME
    materialize: { format: "env", via: "export" }
  - alias: azure_ts_cert_profile_name
    canonical: env:AZURE_TS_CERT_PROFILE_NAME
    materialize: { format: "env", via: "export" }
  - alias: kmp-project-template-upload-keystore
    canonical: "secrets/live/android/keystores/upload_keystore.keystore"
    materialize: { format: "file", via: "copy" }
  - alias: kmp-project-template-upload-keystore-storepass
    canonical: "secrets/live/android/keystores/upload_keystore.properties"
    materialize: { format: "env", via: "export" }
  - alias: kmp-project-template-upload-keystore-keyalias
    canonical: "secrets/live/android/keystores/upload_keystore.properties"
    materialize: { format: "env", via: "export" }
  - alias: kmp-project-template-upload-keystore-keypass
    canonical: "secrets/live/android/keystores/upload_keystore.properties"
    materialize: { format: "env", via: "export" }
  - alias: mifos-x-playstore-service-account
    canonical: "secrets/live/android/play/playStorePublishServiceCredentialsFile.json"
    materialize: { format: "file", via: "copy" }
  - alias: mifos-x-firebase-service-account
    canonical: "secrets/live/android/firebase/firebaseAppDistributionServiceCredentialsFile.json"
