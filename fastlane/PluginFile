# Autogenerated by fastlane
#
# Ensure this file is checked in to source control!
gem 'fastlane-plugin-firebase_app_distribution', '1.0.0'
# AC21 FIX: legacy build-number-increment plugin removed — every callsite in
# fastlane/FastFile now uses the core `increment_build_number` action (shipped
# with fastlane itself). The plugin was dead weight slowing `bundle install`.
