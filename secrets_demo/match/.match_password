# CLAUDE-PLACEHOLDER — do not commit this to secrets/
# Replace with your Match encryption password.
# Generate with: openssl rand -base64 32 > secrets/match/.match_password
PLACEHOLDER_MATCH_ENCRYPTION_PASSWORD
